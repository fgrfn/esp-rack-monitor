# CI implementation

The repository uses a pinned ESPHome build for pull requests, a scheduled compatibility build against the latest ESPHome release, actionlint for workflow validation, and Dependabot for GitHub Actions updates.
